version: '3.3'

services:
  web:
    build: .
    command: sh -c "
      python manage.py makemigrations &&
      python manage.py migrate &&
      python manage.py runserver 0.0.0.0:8000 "
    volumes:
      - ./:/usr/src/app/
    ports:
      - "8000:8000"
    depends_on:
      - db
      - redis
    env_file:
      - ./.env


  db:
    image: postgres:14.0-alpine
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    env_file:
      - ./.env.db
    ports:
      - "5433:5432"


  celery:
    build: .
    command: celery -A internship_car_project worker -B
    volumes:
      - project_data:/usr/src/app/
    env_file:
      - ./.env
    depends_on:
      - web

  beat:
    build: .
    command: celery -A internship_car_project beat -l INFO
    volumes:
      - project_data:/usr/src/app/
    env_file:
      - ./.env
    depends_on:
      - celery

  redis:
    image: redis:alpine
    ports:
      - "127.0.0.1:6379:6379"
    depends_on:
      - db

  flower:
    build: .
    command: celery -A internship_car_project flower --port=5555
    ports:
      - "5555:5555"
    env_file:
      - ./.env
    depends_on:
      - web
      - redis
      - celery
volumes:
  postgres_data:
    driver: local
  project_data: